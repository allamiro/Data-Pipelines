---
# Stage: DataVisualization - Deploy visualization tool (Kibana/Grafana)

- name: Include tool-specific tasks
  include_tasks: "{{ pipeline.stages.visualization.tool }}.yml"

- name: Open firewall ports (if managed)
  include_tasks: firewall.yml
  when: manage_firewall | default(false)
